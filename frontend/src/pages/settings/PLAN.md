# Settings — PLAN

> Files: `index.html` + `settings.js` + `settings.css` + `PLAN.md`.
> Status: `scaffold-pending`.

## 1. Purpose

Workspace preferences: workspace name form + save toast. Future sections
(notifications, members, danger zone with confirmations) attach here.

## 2. Content

Single form-grid section now; writes through `src/services` (no direct DOM in
services, no fetch in page). Validation + success toast per design-system
feedback patterns.

## 3. Non-goals

No billing, no member management in this phase.
