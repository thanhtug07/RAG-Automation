[project]
name = "rag-flow"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["crewai", "crewai[tools]", "litellm", "python-dotenv", "pyyaml", "pydantic>=2"]

[tool.pytest.ini_options]
testpaths = ["."]
