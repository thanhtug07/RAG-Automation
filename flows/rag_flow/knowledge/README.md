# knowledge/ (optional)

Static documents live in repo `data/` (`demo_q3_2024.csv`, `demo_products.json`,
`demo_policies.md`, `demo_reviews.txt`, `demo_customers.csv`, `demo_orders.csv`).
Tools read them directly — nothing is duplicated here.
